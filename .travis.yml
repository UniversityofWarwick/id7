matrix:
  include:
    - language: ruby
      node_js: 
        - lts
      before_install:
        - npm i -g npm
      install:
        - gem install bundler
        - bundler install
        - npm ci
      script:
        - npm run docs-assets
        - npm run test
